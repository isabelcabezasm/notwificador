const chai = require('chai');
const expect = chai.expect;

describe('cognitiveServices', () => {
	let cognitiveServices;

	it('should initialise ok', () => {
		cognitiveServices = require('../index.js');
		expect(cognitiveServices).not.to.be('undefined');
	});

	it('should have many APIs', () => {
		assert.isAbove(Object.keys.length,5);
	});


});